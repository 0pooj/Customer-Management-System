# 🌟 Customer Management System

This is a simple project developed using Python for basic customer data management.

## 📁 Project Structure

* **customers.py**: Contains the main application code and logic.
* **customers.json**: The file used to save and restore customer data.

## ✨ Key Features

* **Data Management:** Stores essential customer information (Name, Email, etc.).
* **Automatic Saving:** All data is saved automatically to a JSON file to ensure persistence after the application is closed.
* **CRUD Operations:** Supports Create (Add), Read (Display), Update (Edit), and Delete operations.

## 🚀 How to Run

1.  **Requirements:** Make sure [Python 3](https://www.python.org/downloads/) is installed.

2.  **Clone the Repository:**
    ```bash
    git clone [https://github.com/0pooj/Customer-Management-System.git](https://github.com/0pooj/Customer-Management-System.git)
    cd Customer-Management-System
    ```

3.  **Run the Application:**
    ```bash
    python customers.py
    ```
